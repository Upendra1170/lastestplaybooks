- name: loops
  hosts: localhost
  tasks:
    - name: print packages
      ansible.builtin.package:
        name: "package: {{ item }}"
        state: present
      loop:
        - git 
        - mysql