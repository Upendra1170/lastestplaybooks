- name: install and run nginx
  host: web
  become: yes
  tasks: 
  - name: install nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  -  name: run inginx
     ansible.builtin.service:
        name:nginx 
        state: started
